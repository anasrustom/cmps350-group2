export const metadata = {
  title: 'QatarConnect',
  description: 'QatarConnect — Phase 2',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
